{% extends 'base.html.twig' %}

{% block title %}Activite{% endblock %}

{% block body %}
    <h1>Activite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdActivite</th>
                <td>{{ activite.idActivite }}</td>
            </tr>
            <tr>
                <th>NomActivite</th>
                <td>{{ activite.nomActivite }}</td>
            </tr>
            <tr>
                <th>TypeActivite</th>
                <td>{{ activite.typeActivite }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ activite.prix }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ activite.adresse }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ activite.pays }}</td>
            </tr>
            <tr>
                <th>ImagePath</th>
                <td>{{ activite.imagePath }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_activite_index') }}">back to list</a>

    <a href="{{ path('app_activite_edit', {'idActivite': activite.idActivite}) }}">edit</a>

    {{ include('activite/_delete_form.html.twig') }}
{% endblock %}
