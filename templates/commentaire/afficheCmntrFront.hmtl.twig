
<section style="background-color: #eee;">
    <div class="container my-5 py-5">
        <div class="box" id="box">

        <div class="row d-flex justify-content-center">
            <div class="col-md-12 col-lg-10 col-xl-8">
                <div class="card">
                    <div class="card-body">
                        <div class="d-flex flex-start align-items-center">

                            <div>
                                <h6 class="fw-bold text-primary mb-1">Commentaires</h6>
                                <p class="text-muted small mb-0">

                                </p>
                            </div>
                        </div>

                        <p class="mt-3 mb-4 pb-2">
                        </p>
                        <img height="500" width="700" src=" {{ asset('uploads/' ~ imgr) }}">

                        <table>
                            {% for message in app.flashes('info') %}
                                <div class="alert alert-danger">
                                    {{ message }}
                                </div>
                            {% endfor %}
                            {% for i in tabCmntr %}


                                <tr>
                                    <tr>

                            </tr>
                                    <td>
                                        {{ i.idUser }}

                                      :  {{ i.contenuCommentaireR }}

                                    </td>
                                    <td>

                                    </td>
                                    <td>
                                <td><a href="{{ path('Upcommenter',{'idcommentairer':i.idcommentairer}) }}"  type="submit" class="btn btn-danger" id="" >Modifier</a></td>

                                </td>
                                </tr>

                            {% endfor %}
                        </table>


                    <div class="card-footer py-3 border-0" id="box">
                        {{ form_start(form) }}
                        <div class="d-flex flex-start w-100">
                            <div class="form-outline w-100">

                                {{form_row(form.contenuCommentairer,{'attr':{'class':'form-control'}})}}

                                <br>
                                <div class="card-footer">
                                    <button type="submit" >Commenter</button>
                                </div>
                            </div>
                        </div>

                        {{ form_end(form) }}
                                </div>
                            </div>

                    </div>
                </div>
            </div>
    </div>
    </div>


</section>
