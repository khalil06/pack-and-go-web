<form method="post" action="{{ path('app_ticket_delete', {'numTicket': ticket.numTicket}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ticket.numTicket) }}">
    <button class="btn">Delete</button>
</form>
